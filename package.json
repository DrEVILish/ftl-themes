{
  "name": "ftl-themes-tooling",
  "private": true,
  "description": "Dev tooling only: rendered audits of the example pages. The library itself has no JS dependencies.",
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit_rendered.mjs --fail-below 4.5",
    "audit:layers": "node scripts/audit_layers.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
